import styled from "styled-components";

export const ContainerPieceOfCode = styled.div`
    width: 50rem;
    background: ${props => props.theme["base-post"]};
    padding: 1rem;
    border-radius: 8px;
    display: flex;
    gap: 1rem;
`